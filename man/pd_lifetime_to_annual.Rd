% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pd_horizon.R
\name{pd_lifetime_to_annual}
\alias{pd_lifetime_to_annual}
\title{Convert a cumulative lifetime PD to an equivalent annual PD}
\usage{
pd_lifetime_to_annual(pd, term)
}
\arguments{
\item{pd}{Numeric vector of cumulative lifetime PDs in `[0, 1]`.}

\item{term}{Numeric vector (recycled) of horizons in years, `> 0`.}
}
\value{
Numeric vector of equivalent annual PDs in `[0, 1]`. `NA` inputs
  propagate; `term <= 0` yields `NA` with a warning.
}
\description{
Under a constant-hazard (flat marginal) assumption, a cumulative probability
of default `pd` over `term` years implies a per-year PD of
\eqn{1 - (1 - pd)^{1/term}}. Use this to place a TRISK term-structured PD into
a 12-month IFRS-9 Stage 1 slot.
}
\examples{
pd_lifetime_to_annual(0.10, term = 5)   # ~0.0209
}
\seealso{
[pd_annual_to_lifetime()]
}
