% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrate.R
\name{integrate_el}
\alias{integrate_el}
\title{Integrate TRISK EL shift into an internal EL estimate}
\usage{
integrate_el(
  analysis_data,
  internal_el = NULL,
  method = c("absolute", "relative")
)
}
\arguments{
\item{analysis_data}{Data frame from [run_trisk_on_portfolio()]; must contain
columns `expected_loss_baseline`, `expected_loss_shock`.}

\item{internal_el}{Numeric vector of length `nrow(analysis_data)`, or a data
frame with `company_id` + `internal_el` columns, or NULL (default) which
uses `expected_loss_baseline`.}

\item{method}{One of "absolute", "relative". Default "absolute".}
}
\value{
List with `$portfolio`, `$portfolio_long`, `$aggregate`.
}
\description{
Applies one of two methods to translate the TRISK baseline-to-shock EL change
into the bank's own internal EL scale. Mirrors the Shiny EL integration logic.
}
