% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_portfolio.R, R/run_trisk_on_portfolio.R
\name{join_trisk_outputs_to_portfolio}
\alias{join_trisk_outputs_to_portfolio}
\title{Join TRISK Outputs to Portfolio}
\usage{
join_trisk_outputs_to_portfolio(portfolio_data, npv_results, pd_results)

join_trisk_outputs_to_portfolio(portfolio_data, npv_results, pd_results)
}
\arguments{
\item{portfolio_data}{Data frame containing portfolio information.}

\item{npv_results}{Data frame containing NPV (Net Present Value) results from TRISK model.}

\item{pd_results}{Data frame containing PD (Probability of Default) results from TRISK model.}
}
\value{
A data frame of portfolio data joined with TRISK model outputs.

A data frame of portfolio data joined with TRISK model outputs.
}
\description{
This function joins the TRISK model outputs (NPV and PD results) to the portfolio data.

This function joins the TRISK model outputs (NPV and PD results) to the portfolio data.
}
\examples{
# Example usage (not run):
# joined_data <- join_trisk_outputs_to_portfolio(portfolio_data, npv_results, pd_results)
# Example usage (not run):
# joined_data <- join_trisk_outputs_to_portfolio(portfolio_data, npv_results, pd_results)
}
