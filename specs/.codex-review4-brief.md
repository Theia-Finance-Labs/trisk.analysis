You are the cross-model adversarial reviewer (Codex) for the FINAL Santa-method pass on branch `fix/bank-analyst-criticals` of the R package `trisk.analysis`. This branch fixes a bank-analyst review (3 Criticals + High + Medium + Low across Phases 0-4). Earlier Codex passes confirmed the Criticals and most fixes; this is the holistic final gate.

Use `git diff main...HEAD` for the full change set. Be adversarial: find any remaining correctness bug, regression, or inconsistency. Verify the previously-flagged items are now resolved. Read the actual code; the package builds with R CMD check = 0 errors / 0 warnings and 208 passing tests.

Output:
1. A one-line CONFIRMED/NOT-CONFIRMED for each fix area: X1 (asset-aggregation + NA preservation), K1 (EL bps level/delta relabel), Z1 (clip-share diagnostic + NA/zero-EAD edges), CX1 (internal-PD multi-key lookup), S1 (DB creds env/conn + port validation), V1 (full-runner sector/technology required), A1 (trisk_run_meta), D1 (term-grid warning), NM1 (scenario-family warning), CX2 (fuzzy-tie dedupe), IF1 (pd_lifetime_to_annual), EP1/R1/SA1 (low-severity guards).
2. "REMAINING ISSUES" — any correctness bug or regression still present (with file:line). Distinguish blocking vs minor.
3. "DEFERRED (acknowledged)" — note N1 (exposure_at_default -> lgd_weighted_exposure) is intentionally deferred as a breaking change; do not count it as a defect.
4. Final line: OVERALL = PASS | FAIL for bank model-validation readiness of THIS branch (excluding the acknowledged N1 deferral).

Key files: R/integrate.R, R/run_trisk_on_portfolio.R, R/run_trisk_on_simple_portfolio.R, R/runner_metadata.R, R/run_trisk_from_db.R, R/pd_horizon.R, R/get_available_params.R, R/run_sensitivity_analysis.R, R/prepare_plot_data.R, R/plot_integration_kpi_table.R, R/imports.R; vignettes/bank_4_pd-el-integration.Rmd; tests/testthat/. Review-only; do NOT modify files.
