You are the cross-model adversarial reviewer (Codex) in a Santa-method review. This is the THIRD pass, covering the Phase 2 HIGH-severity fixes on branch `fix/bank-analyst-criticals`. Verify each fix against the actual code; be adversarial — try to prove each is WRONG, INCOMPLETE, or that it INTRODUCED a regression. (X1/K1/Z1 Criticals were reviewed already; focus on the five items below, but flag any Critical regression you notice.)

Use `git diff main...HEAD` for the changes. Output per item: VERDICT = FIXED | INCOMPLETE | REGRESSED with 2-5 lines of evidence citing files/lines. Then "NEW ISSUES INTRODUCED". End with: OVERALL = PASS | FAIL. Review-only; do NOT modify files.

## CX1 — internal PD/EL lookup keyed on all shared keys
File: R/integrate.R `resolve_internal_series()`. Change: value column = the one column not in analysis_data; key columns = those shared with analysis_data (company_id [+ term/...]); composite character key match; errors on duplicate keys (anyDuplicated). Challenge: (a) Does it preserve the prior company_id-only broadcast behaviour? (b) Type-safety of the \r-joined character key (e.g. integer vs character term/company_id)? (c) Could a value column legitimately named like an analysis column be misclassified as a key (silently no value col / wrong error)? (d) NA in key columns. (e) The pre-existing test expecting a "company_id" error on a no-company_id lookup — still satisfied?

## S1 — de-hardcode DB credentials
File: R/run_trisk_from_db.R. Change: optional `conn`; else `connect_trisk_db_from_env()` reads TRISK_DB_* and fails fast; on.exit disconnect only when we opened the connection. Challenge: (a) Any hardcoded secret left? (b) Is the caller-supplied `conn` wrongly disconnected? (c) Port parse (as.integer of env string)? (d) Does it error BEFORE attempting a connection when secrets are missing? (e) Information leak in the error message?

## V1 — full runner requires sector/technology; simple runner does not
Files: R/run_trisk_on_portfolio.R `check_portfolio()` (now requires sector+technology); R/run_trisk_on_simple_portfolio.R `check_portfolio_simple()` (unchanged). Challenge: (a) Does requiring sector+technology break any LEGITIMATE full-runner path — the by-name fuzzy-match path or the country-aggregate path (company_id & company_name NA)? Trace whether those paths can supply sector/technology. (b) Is the simple runner genuinely unaffected? (c) Does bundled test/vignette portfolio data still satisfy the new requirement?

## A1 — audit-trail metadata
Files: R/runner_metadata.R `build_trisk_run_meta()`; attached as attribute `trisk_run_meta` in both runners. Challenge: (a) Is `run_id` captured correctly (unique(na.omit(pd_results$run_id)))? (b) Does `list(...)` capture the forwarded TRISK args correctly in each runner? (c) Could attaching the attribute break downstream dplyr ops or the documented return shapes (full runner returns a data.frame; simple returns a 2-element list)? (d) packageVersion failure handling.

## D1 — term-outside-grid warning
File: R/runner_metadata.R `warn_terms_outside_grid()`, called in both runners after pd_results. Challenge: (a) FALSE POSITIVES — does it warn for rows that are actually fine (e.g. an unmatched company whose terms ARE in the grid), or conflate "term not in grid" with "company not matched"? (b) Is the global grid `unique(pd_results$term)` the right reference vs per-company? (c) company_id NA in the by-name path before matching — misleading message? (d) Does it ever throw instead of warn?

Files to read: R/integrate.R, R/run_trisk_from_db.R, R/run_trisk_on_portfolio.R, R/run_trisk_on_simple_portfolio.R, R/runner_metadata.R, and the tests under tests/testthat/ (test-internal-lookup, test-run-trisk-from-db, test-check-portfolio, test-runner-metadata).
