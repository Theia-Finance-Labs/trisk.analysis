You are the cross-model adversarial reviewer (Codex) in a Santa-method review. This is the SECOND pass: verify the FIXES for three Criticals (X1, K1, Z1) you previously confirmed in trisk.analysis. Review the diff of branch `fix/bank-analyst-criticals` vs `main`. Be adversarial: try to prove each fix is WRONG, INCOMPLETE, or that it INTRODUCED a regression. Read the actual code; do not trust this summary.

Per finding output: VERDICT = FIXED | INCOMPLETE | REGRESSED, with 2-5 lines of evidence citing files/lines. Then "NEW ISSUES INTRODUCED" (anything the diff breaks). End with: OVERALL = PASS | FAIL.

Review-only. Do NOT modify files.

## X1 fix to verify — asset aggregation before the portfolio join
Change: `run_trisk_on_portfolio.R` adds `aggregate_npv_across_assets(npv_results)` which groups by (run_id, company_id, sector, technology, country_iso2) and sums net_present_value_baseline/shock, called inside `join_trisk_outputs_to_portfolio()` before the NPV×PD join. Claim: a company with >1 asset in one (sector, technology) no longer fans one loan into N rows at full exposure (was 2x, now 1x).
Challenge: (a) Does dropping asset_id/asset_name/model-derived npv difference/change columns break any downstream consumer (plots, compute_analysis_metrics, the country-aggregate branch in join_trisk_outputs_to_portfolio that calls aggregate_facts_trisk)? (b) Is summing NPV across assets the correct aggregation (vs mean/weighted)? (c) Does it correctly handle NA NPV, and companies spanning multiple countries for the same technology? (d) Could the collapse now MERGE two distinct loans that share (company, sector, technology, term) and hide them? Trace compute_analysis_metrics() and the plot pipeline.

## K1 fix to verify — EL bps relabel, level headlined
Change: `aggregate_el_integration()` now emits both `el_adjusted_bps` (level) and `el_adjustment_bps` (delta = adjusted - internal), both = el_to_bps(x, total_exposure_usd) (raw exposure). KPI table (`plot_integration_kpi_table.R`) headlines `Adjusted EL (bps)` (level) with `EL/exposure delta (bps)` secondary; sector breakdown shows the adjusted-level rate; bank_4 P3b/P4 text updated.
Challenge: (a) Are both bps values arithmetically what the labels claim? (b) Is raw exposure the right denominator (vs exposure_at_default = exposure*LGD)? Confirm EL/EAD would be PD-in-bps. (c) Any column the KPI/breakdown table references that no longer exists or is mislabelled? (d) Does headlining the LEVEL (a deliberate user choice) remain defensible given the label says "Adjusted EL", not "delta"?

## Z1 fix to verify — clip diagnostic + warning
Change: `integrate.R` adds `zscore_clipped_share()` (fraction of rows where internal/baseline/shock lie outside [floor,cap]) and `warn_if_overclipped()` (warns above ZSCORE_CLIP_WARN_THRESHOLD=0.5). integrate_pd attaches it for method=zscore using (internal,baseline,shock); integrate_el using effective PDs |EL|/ead. Floor unchanged.
Challenge: (a) Is the clipped-share computed on the SAME quantities that get clipped in apply_pd_method/apply_el_method (PD path: raw PDs; EL path: |EL|/ead)? Any mismatch? (b) Edge cases: ead==0/NA rows, all-NA series, single-row input. (c) Does the warning misfire or under-fire? (d) Is the diagnostic only attached for zscore (not absolute/relative)?

Files: R/run_trisk_on_portfolio.R, R/integrate.R, R/imports.R, R/plot_integration_kpi_table.R, R/prepare_plot_data.R, vignettes/bank_4_pd-el-integration.Rmd. Use `git diff main...HEAD` to see the changes.
